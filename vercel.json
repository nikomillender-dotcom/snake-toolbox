{
  "$comment": "Deploy posture (I5). require-corp is the ONLY supported COEP value (iPad Safari has no credentialless). CSP script-src uses wasm-unsafe-eval; add unsafe-eval ONLY if the pinned Pyodide 314.0.2 genuinely requires it (hands-on verify at deploy, checklist item 2).",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Cross-Origin-Opener-Policy", "value": "same-origin" },
        { "key": "Cross-Origin-Embedder-Policy", "value": "require-corp" },
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        {
          "key": "Content-Security-Policy",
          "value": "default-src 'self'; script-src 'self' 'wasm-unsafe-eval'; connect-src 'self' https://api.github.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'none'"
        }
      ]
    }
  ]
}
